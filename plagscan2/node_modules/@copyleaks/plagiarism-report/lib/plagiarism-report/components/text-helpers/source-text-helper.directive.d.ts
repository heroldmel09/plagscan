import { AfterContentInit, OnDestroy } from '@angular/core';
import { ContentMode, Match, ScanResult, ScanSource } from '../../models';
import { HighlightService } from '../../services/highlight.service';
import { ReportService } from '../../services/report.service';
import * as i0 from "@angular/core";
export declare class SourceTextHelperDirective implements AfterContentInit, OnDestroy {
    private highlightService;
    private reportService;
    host: {
        currentPage: number;
        textMatches: any;
    };
    constructor(highlightService: HighlightService, reportService: ReportService);
    private children;
    private current;
    /**
     * Life-cycle method
     * - subscribe to text match clicks from suspect
     * - subscribe to jump events
     * - subscribe to the source text selected match
     */
    ngAfterContentInit(): void;
    /**
     * Handle a match click that was broadcasted by the suspect text helper
     * @param elem the broadcasted element
     * @param suspect the suspected scan result
     */
    handleBroadcast(match: Match, source: ScanSource, suspect: ScanResult, contentMode: ContentMode): void;
    /**
     * Handles the jump logic
     * @param forward the direction of the jump
     */
    handleJump(forward: boolean): void;
    /**
     * Checks whether it is possible to jump forward/backward in the current page
     * @param forward the direction of the jump
     */
    canJumpInCurrentPage(forward: boolean): boolean;
    /**
     * Life-cycle method
     * empty for `untilDestroy` rxjs operator
     */
    ngOnDestroy(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<SourceTextHelperDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<SourceTextHelperDirective, "[crSourceTextHelper]", never, { "host": "host"; }, {}, ["children"]>;
}

import { AfterContentInit, OnDestroy } from '@angular/core';
import { HighlightService } from '../../services/highlight.service';
import { ReportService } from '../../services/report.service';
import * as i0 from "@angular/core";
export declare class OriginalTextHelperDirective implements AfterContentInit, OnDestroy {
    private reportService;
    private highlightService;
    host: {
        textMatches: any;
        currentPage: number;
    };
    private lastSelectedOriginalTextMatch;
    constructor(reportService: ReportService, highlightService: HighlightService);
    private children;
    private current;
    /**
     * Handles the jump logic
     * @param forward the direction of the jump
     */
    handleJump(forward: boolean): void;
    /**
     * Checks whether it is possible to jump forward/backward in the current page
     * @param forward the direction of the jump
     */
    canJumpInCurrentPage(forward: boolean): boolean;
    /**
     * Life-cycle method
     * - subscribe to text jump clicks
     * - subscribe to original text selected match state
     */
    ngAfterContentInit(): void;
    /**
     * Life-cycle method
     * empty for `untilDestroy` rxjs operator
     */
    ngOnDestroy(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<OriginalTextHelperDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<OriginalTextHelperDirective, "[crOriginalTextHelper]", never, { "host": "host"; }, {}, ["children"]>;
}

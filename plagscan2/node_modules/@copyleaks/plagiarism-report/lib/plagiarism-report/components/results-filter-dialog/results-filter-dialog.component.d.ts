import { OnDestroy, OnInit } from '@angular/core';
import { MatDialogRef } from '@angular/material/dialog';
import { Subscription } from 'rxjs';
import { ResultPreview } from '../../models';
import { ReportService } from '../../services/report.service';
import { CopyleaksTranslateService, CopyleaksTranslations } from '../../services/copyleaks-translate.service';
import { DirectionService } from '../../services/direction.service';
import { LiveAnnouncer } from '@angular/cdk/a11y';
import * as i0 from "@angular/core";
export declare class ResultsFilterDialogComponent implements OnInit, OnDestroy {
    private translatesService;
    private dialogRef;
    directionService: DirectionService;
    private reportSerivce;
    private announcer;
    query: string;
    hidden: string[];
    hiddenSubscription: Subscription;
    results: ResultPreview[];
    translations: CopyleaksTranslations;
    checkedAllResults: boolean;
    constructor(translatesService: CopyleaksTranslateService, dialogRef: MatDialogRef<ResultsFilterDialogComponent>, directionService: DirectionService, reportSerivce: ReportService, announcer: LiveAnnouncer);
    /**
     * Checks whether the hidden results contain a `result`
     * @param result the result to test
     */
    isHidden(result: ResultPreview): boolean;
    /**
     * Adds or removes the `result` from the hidden results list essentialy checks or unchecks it
     * @param result the result to check/uncheck
     */
    toggleResult(result: ResultPreview): void;
    /**
     * Checks or unchecks all results
     */
    checkAll(): void;
    /**
     * Closes the dialog while indicating whether to `save` changes or not.
     */
    close(save?: boolean): void;
    /**
     * Saves the hidden results changes
     */
    saveChanges(): void;
    /**
     * Life-cycle method
     * subscribe to:
     * - result previews
     * - user defined hidden results
     * - dialog events
     */
    ngOnInit(): void;
    /**
     * Life-cycle method
     * empty for `untilDestroy` rxjs operator
     */
    ngOnDestroy(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ResultsFilterDialogComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<ResultsFilterDialogComponent, "cr-results-filter-dialog", never, {}, {}, never, never>;
}
